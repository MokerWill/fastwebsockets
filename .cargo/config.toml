[target.aarch64-unknown-linux-gnu]
rustflags = [
    "-C", "target-cpu=native",
    "-C", "link-arg=-s",
    "-C", "relocation-model=pic"
]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
